{% extends '::base.html.twig' %}

{% block body %}
    <main>
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <h1>{{ "remoteDesktop.index.headline"|trans }}</h1>
                    {% for remoteDesktop in remoteDesktops %}
                        <div class="well">
                            {{ remoteDesktop.id }}
                            <br>
                            {{ remoteDesktop.kind }}
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
